/**
 *
 * TeamMatchesList
 *
 */

import React, { memo } from 'react';
// import PropTypes from 'prop-types';
// import styled from 'styled-components';

function TeamMatchesList() {
  return <div />;
}

TeamMatchesList.propTypes = {};

export default memo(TeamMatchesList);
